name: Python application

on:
  push:
    branches: [ main, dev ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install cx_Freeze

    - name: Build executable
      run: |
        python setup.py build

    - name: Get Date and Time
      id: date
      run: |
        echo "RELEASE_TIME=$(Get-Date -Format 'MM.dd.HHmm')" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
      shell: pwsh

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: release-${{ github.sha }}
        release_name: Beta Ver 0.${{ env.RELEASE_TIME }}.cx
        draft: false
        prerelease: false

    - name: Upload Executable to Artifacts
      uses: actions/upload-artifact@v2
      with:
        name: installer
        path: build/exe.win-amd64-3.x/installer

    - name: Upload Executable to Release
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: build/exe.win-amd64-3.x/installer
        asset_name: installer
        asset_content_type: application/octet-stream